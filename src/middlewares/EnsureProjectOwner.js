/* migrated from app/Http/Middleware/EnsureProjectOwner.php */\\nexport function EnsureProjectOwner(req, res, next) {\\n  const projectId = req.params.project; // TODO: Asegurar que req.params.project esté correctamente poblado\\n\\n  // TODO: Implementar la obtención del proyecto desde la base de datos\\n  // const project = await Project.findById(projectId);\\n  const project = { user_id: null }; // Reemplazar con la lógica real\\n\\n  if (!project) {\\n    return res.status(404).json({ error: 'Proyecto no encontrado' });\\n  }\\n\\n  if (project.user_id !== req.user.id) { // TODO: Asegurar que req.user esté correctamente poblado\\n    return res.status(403).json({ error: 'No autorizado.' });\\n  }\\n\\n  req.project = project; // Adjuntar el proyecto a la solicitud\\n  return next();\\n}

export default { EnsureProjectOwner };